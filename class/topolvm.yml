parameters:
  kapitan:
    compile:
      - input_paths:
          - topolvm/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - topolvm/component/main.jsonnet
        input_type: jsonnet
        output_path: topolvm/
