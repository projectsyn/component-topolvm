apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/instance: topolvm
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: topolvm
    app.kubernetes.io/version: 0.19.0
    helm.sh/chart: topolvm-11.2.1
  name: topolvm-webhook-ca
  namespace: syn-topolvm
spec:
  commonName: ca.webhook.topolvm
  duration: 8760h0m0s
  isCA: true
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: topolvm-webhook-selfsign
  secretName: topolvm-webhook-ca
  usages:
    - digital signature
    - key encipherment
    - cert sign
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/instance: topolvm
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: topolvm
    app.kubernetes.io/version: 0.19.0
    helm.sh/chart: topolvm-11.2.1
  name: topolvm-mutatingwebhook
  namespace: syn-topolvm
spec:
  dnsNames:
    - topolvm-controller
    - topolvm-controller.syn-topolvm
    - topolvm-controller.syn-topolvm.svc
  duration: 8760h0m0s
  issuerRef:
    group: cert-manager.io
    kind: Issuer
    name: topolvm-webhook-ca
  secretName: topolvm-mutatingwebhook
  usages:
    - digital signature
    - key encipherment
    - server auth
    - client auth
